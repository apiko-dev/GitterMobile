import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  tabText: {
    textAlign: 'center',
    fontSize: 24,
    marginTop: 50
  }
})

export default styles
