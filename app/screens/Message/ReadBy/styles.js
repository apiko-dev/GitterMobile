import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  container: {
    // flex: 1
  },
  usersContainer: {
    padding: 16,
    alignItems: 'center',
    flexWrap: 'wrap',
    flexDirection: 'row'
  },
  itemContainer: {
    marginRight: 6,
    marginBottom: 4
  }
})

export default styles
